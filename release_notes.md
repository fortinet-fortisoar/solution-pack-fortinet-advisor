### What’s New

#### Bug Fixes:
- Fixed an issue that caused user prompts on a module's record to fail when the module had no correlated field or key mapping in the Key Store "fortiai-static-questions", but the recommendation engine was enabled.
- Optimized performance for a module with relevant fields.